/**
 * 视频详情页下方的标签栏和评论（用户-视频信息）
 */
import {Box, Typography} from "@mui/material";

export default function VideoTagComment({videoName, avatar, username, followers, createTime, likes}) {


    return (
        <Box>
            <Typography variant="h3">
                KL
            </Typography>

        </Box>
    )
}